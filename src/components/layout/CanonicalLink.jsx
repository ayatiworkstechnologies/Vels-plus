// components/seo/CanonicalLink.jsx
// Note: In Next.js App Router, canonical links are handled via the metadata object (`alternates.canonical`).
// This component returns null to avoid invalid DOM placement inside <html>.
import React from 'react';

const CanonicalLink = ({ path }) => {
  return null;
};

export default CanonicalLink;